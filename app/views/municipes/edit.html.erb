<main class="container">
  <%= link_to "Voltar", municipe_path(@municipe) %>

  <div class="header">
    <h1>Editar municipe</h1>
  </div>

  <%= turbo_frame_tag @municipe do %>
    <%= render "form", municipe: @municipe %>
  <% end %>
</main>